# Sprint 166 Day 7: Hosted CI Evidence Reconciliation

## Purpose

Day 7 reconciles hosted CI evidence with the local and advisory evidence
recorded on Days 5 and 6. It also closes the hosted comparison artifact-scope
mismatch found during Days 2, 4, and 6: the Linux hosted comparison lane ran
the maintained multi-family comparison freshness target but still described
and uploaded only QR minimum-norm artifacts.

## Workflow Change

Updated `.github/workflows/ci.yml` in the Linux reviewed hosted
oracle/comparison freshness job:

- renamed the comparison run/summary/upload steps from QR-minnorm-specific
  wording to selected-comparison wording;
- documented the reviewed hosted comparison surface as the maintained selected
  families regenerated by `make report-index-comparison-freshness`;
- summarized QR min-norm, QR compatible least-squares, and partial-SVD diag6 k2
  comparison outputs;
- uploaded all selected comparison artifacts from:
  - `build/comparison/qr_minnorm/`
  - `build/comparison/qr_compatible_ls/`
  - `build/comparison/partial_svd_diag6_k2/`

This is a CI evidence-scope correction. It does not widen comparison claims
beyond the selected generated report rows and fixture-scoped comparison
contracts.

## Validation Performed

| Command or check | Result | Notes |
| --- | --- | --- |
| `make report-index-comparison-freshness` | Pass | Regenerated QR min-norm, QR compatible least-squares, and partial-SVD diag6 k2 comparison outputs; freshness passed for 25 rows. |
| Updated hosted comparison summary logic | Pass | Reported 3 selected targets, 22 selected rows, and 22 passing rows. |
| Selected comparison upload path check | Pass | Every path listed in the updated upload-artifact block exists after the freshness target runs. |
| `git diff --check` | Pass | No whitespace errors reported. |

The updated local summary output was:

| Target | Fixture | Selected rows | Passing rows | Optional deferred dependencies |
| --- | --- | ---: | ---: | --- |
| `qr-minnorm` | `qr_underdetermined_minnorm_2x4` | 6 | 6 | `numpy`, `scipy` |
| `qr-compatible-ls` | `qr_overdetermined_compatible_5x3` | 6 | 6 | `numpy`, `scipy` |
| `partial-svd-diag6-k2` | `partial_svd_diag6_k2` | 10 | 10 | `numpy`, `scipy` |

## Hosted CI Lane Map

| Workflow | Job | Evidence tier | Evidence owned | Boundaries |
| --- | --- | --- | --- | --- |
| `.github/workflows/ci.yml` | `Linux enforced reviewed CMake parity path` | Reviewed hosted | CMake configure/build/CTest parity through `make quality-review-cmake`. | Linux hosted CMake proof only; not Windows/macOS parity. |
| `.github/workflows/ci.yml` | `Linux reviewed static-first package contract` | Reviewed hosted | Unix Make install/`pkg-config`, CMake install/export, and static-first deferral proof. | Static archive package contract only; no shared library, dynamic ABI, runtime-loader, package-manager, or Windows parity claim. |
| `.github/workflows/ci.yml` | `Linux reviewed hosted oracle/comparison freshness` | Reviewed hosted | Selected oracle freshness and selected comparison freshness for QR min-norm, QR compatible LS, and partial-SVD diag6 k2 artifacts. | Selected generated rows only; no broad solver or external-library parity. |
| `.github/workflows/ci.yml` | `Linux enforced reviewed Makefile compile-quality path` | Reviewed hosted | Makefile compile-quality path through `make quality-review-compile`. | Compile/lint quality gate only; not a runtime or performance proof. |
| `.github/workflows/ci.yml` | `Linux enforced dead-code report and completeness path` | Reviewed hosted | Dead-code report generation and completeness check. | Source-usage/completeness proof only; not behavioral correctness. |
| `.github/workflows/ci.yml` | `Linux supplemental runtime and bench-fast path` | Supplemental hosted | Direct runtime suite, UBSan, ASan, benchmark binary build, and fast benchmark subset. | Supplemental signal; benchmark subset is not portable performance proof. |
| `.github/workflows/ci.yml` | `Linux supplemental ThreadSanitizer coverage` | Supplemental hosted | Thread and selected eigensolver TSan/OpenMP signal. | Suppression-bounded TSan signal; not full OpenMP runtime proof. |
| `.github/workflows/ci.yml` | `Linux supplemental coverage report` | Supplemental hosted | lcov report generation and threshold check. | Coverage signal only; uploaded HTML is generated artifact evidence. |
| `.github/workflows/macos-ci.yml` | `macOS enforced Apple Clang reviewed path + supplemental GCC leg` | Reviewed plus supplemental hosted | Apple Clang reviewed Make/CMake/wall/sanitizer path; Homebrew GCC supplemental direct build/test. | Apple Clang reviewed scope plus supplemental GCC; no broad macOS platform parity. |
| `.github/workflows/macos-ci.yml` | `macOS reviewed static-first install and pkg-config proof` | Reviewed hosted | macOS Make install and `pkg-config` proof. | Static archive package contract only; no package-manager or shared-library claim. |
| `.github/workflows/macos-ci.yml` | `macOS reviewed static-first CMake install/export proof` | Reviewed hosted | macOS CMake install/export proof and static package deferral guard. | Static archive CMake package only; no dynamic ABI or runtime-loader claim. |
| `.github/workflows/windows-ci.yml` | `Windows enforced reviewed CMake consumer subset (MSVC)` | Reviewed hosted | Windows CMake configure/build, `ctest -N` count, and full hosted MSVC CTest execution. | CMake-first reviewed scope; no Makefile parity or `pkg-config` execution parity. |
| `.github/workflows/windows-ci.yml` | `Windows reviewed CMake install/downstream validation path` | Reviewed hosted | Installed static `.lib`, headers, CMake package metadata, metadata-only `sparse.pc` inspection, downstream consumers, exact-version behavior, mismatch rejection, and no shared/loader metadata. | CMake install/downstream scope only; no Windows Make install, Windows `pkg-config` execution, package-manager, shared-library, dynamic ABI, or runtime-loader claim. |

## Advisory And Local-Only Evidence

| Evidence type | Status |
| --- | --- |
| Source-controlled report-index contract rows | Advisory/source-controlled; governed by schema, Git review, and freshness checks where selected. |
| Local generated API HTML | Local-only unless a hosted publication workflow explicitly owns it. |
| Local generated oracle/comparison rows | Local-only unless generated by the reviewed hosted freshness lane and uploaded by that lane. |
| Local benchmark/sentinel reports | Local-only and methodology-bound; not portable performance or backend-superiority proof. |
| Package report-index rows | Advisory/source-controlled owner rows plus install-validation proof where reviewed lanes execute the package scripts. |

## Unresolved Hosted Dependencies And Residuals

- Hosted workflow success for this branch is not available until the branch is
  pushed and CI runs.
- Windows Makefile install/uninstall parity remains deferred.
- Windows `pkg-config` command execution parity remains deferred; Windows
  `sparse.pc` handling is metadata-only in the reviewed CMake install lane.
- Shared-library support, dynamic ABI compatibility, runtime-loader behavior,
  package-manager distribution, Linux SONAME, macOS install-name/RPATH, and
  Windows DLL/import-library support remain product residuals.
- Generated API HTML hosting/publication remains local-only unless future work
  adds a reviewed hosted publication lane.
- External-library comparisons remain selected fixture comparisons only; no
  broad LAPACK, SuiteSparse, Eigen, SciPy, NumPy, or state-of-the-art parity is
  claimed.

## Support-Tier Wording Notes

Future public claim and closeout wording should say:

- Linux is the source-of-truth reviewed baseline for Makefile compile quality,
  CMake parity, dead-code completeness, static-first package proof, and
  selected hosted oracle/comparison freshness.
- macOS has reviewed Apple Clang and static-first package validation lanes,
  plus a supplemental Homebrew GCC leg.
- Windows has reviewed CMake-first build/test/install/downstream validation,
  including promoted portable tests and static package metadata checks.
- Local generated reports, advisory report-index rows, and local benchmark
  outputs are not hosted proof by themselves.

Future wording should not say:

- broad platform parity;
- portable performance superiority;
- backend superiority;
- broad external-library parity;
- state-of-the-art sparse linear algebra status;
- package-manager support;
- shared-library support;
- dynamic ABI or runtime-loader support;
- Windows Makefile or Windows `pkg-config` execution parity.

## Completion Check

| Criterion | Status | Evidence |
| --- | --- | --- |
| Hosted CI lane ownership is mapped. | Complete | Linux, macOS, and Windows workflow jobs are classified by evidence tier and scope. |
| Reviewed/supplemental/advisory boundaries are separated. | Complete | Lane map and advisory/local-only evidence table separate hosted proof from generated and source-controlled advisory rows. |
| Hosted comparison artifact-scope mismatch is reconciled. | Complete | `.github/workflows/ci.yml` now summarizes and uploads all selected comparison families run by `make report-index-comparison-freshness`. |
| Unresolved hosted dependencies are recorded. | Complete | Remaining Windows Make/`pkg-config`, shared-library, ABI, package-manager, generated API hosting, and broad parity residuals are listed. |
