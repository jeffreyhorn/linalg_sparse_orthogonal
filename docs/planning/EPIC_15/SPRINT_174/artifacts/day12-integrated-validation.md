# Sprint 174 Day 12: Integrated Comparison Validation

## Purpose

Run the selected-family generation, freshness, focused tests, report checks,
claim scans, and package/static deferral guards together after the Day 8
through Day 11 implementation and documentation changes.

## End-To-End Freshness Gate

Ran:

```text
make report-index-comparison-freshness
```

The target regenerated all selected local comparison artifacts:

```text
build/comparison/qr_minnorm/study.tsv
build/comparison/qr_compatible_ls/study.tsv
build/comparison/partial_svd_diag6_k2/study.tsv
build/comparison/lu_nonsym_square_5/study.tsv
```

It then ran selected comparison freshness and completed with:

```text
normalize-report-index: freshness ok (32 rows)
report-index-comparison-freshness: passed (local-only generated comparison freshness)
```

## Focused Tests

Ran:

```text
python3 tests/test_lu_external_dense_reference.py
python3 tests/test_run_external_comparison.py
python3 tests/test_normalize_report_index.py
python3 scripts/run_external_comparison.py --self-check
```

All passed.

Coverage from these checks:

- LU dense-reference helper fixture matrix, RHS, CLI, solution, and unknown
  fixture failure behavior;
- external-comparison runner target registration, output files, manifests,
  dependency rows, row IDs, metrics, pass statuses, support tiers, and
  report-family metadata;
- normalized report-index selected comparison complete-set acceptance and
  fail-closed behavior for row mismatches, duplicates, stale rows, failed rows,
  and skip/defer selected rows;
- exact selected row ID derivation through runner self-check.

## Report Freshness

Ran the direct selected comparison freshness check after the Make regeneration:

```text
python3 scripts/normalize_report_index.py --family comparison --require-generated comparison --check-freshness
```

It completed with:

```text
normalize-report-index: freshness ok (32 rows)
```

The LU rows were reported fresh against the current source commit:

```text
comparison_lu_nonsym_square_5_project_status_v1
comparison_lu_nonsym_square_5_baseline_status_v1
comparison_lu_nonsym_square_5_residual_norm_v1
comparison_lu_nonsym_square_5_solution_norm_v1
comparison_lu_nonsym_square_5_solution_values_v1
comparison_lu_nonsym_square_5_project_vs_baseline_max_abs_delta_v1
```

## Claim Scans

Ran stale selected-comparison wording scan over maintained public and
maintainer documentation:

```text
rg -n "selected QR (and|\\+) partial-SVD comparison|selected QR and partial-SVD comparison|selected QR \\+ partial-SVD comparison|three source-controlled comparison|22 generated comparison|three fixture-local comparison|QR and partial-SVD comparison reports|QR plus partial-SVD comparison" README.md docs/maintainer_guide.md docs/solver_selection.md tests/corpus/README.md tests/corpus/schemas/report_index_fields.md benchmarks/README.md
```

No maintained public/maintainer documentation matches remained.

Ran broad-claim/non-claim scan:

```text
rg -n "state-of-the-art evidence|state-of-the-art status|broad LU|nonsymmetric solve|LU CSR parity|package-manager support|shared-library ABI support" README.md docs/maintainer_guide.md docs/solver_selection.md tests/corpus/README.md tests/corpus/schemas/report_index_fields.md benchmarks/README.md
```

Matches were bounded non-claims or fixture-local LU evidence statements, not
unsupported support claims.

## Package And ABI Deferral Guards

Ran:

```text
bash scripts/package_manager_deferral_check.sh
bash scripts/static_package_deferral_check.sh
```

Both passed. The Day 11 documentation changes did not promote package-manager
support, shared-library support, ABI stability, runtime-loader support, or
dynamic linking claims.

## Quality Gate Scope

No `.c` or `.h` source files were modified. The full C gate
`make format && make lint && make test` is not required for Day 12. The changed
surfaces are Python runner/test code, Make/report-index wiring, documentation,
and planning artifacts, all covered by the focused checks above.

## Result

Sprint 174's selected LU comparison now validates end to end:

- source-controlled helper and fixture tests pass;
- runner target generation passes;
- generated artifacts are regenerated by the owning Make target;
- normalized selected comparison freshness passes with 32 rows;
- missing/stale/invalid selected comparison behavior remains fail-closed in
  focused tests;
- maintained docs name the exact fixture-local LU comparison and retain
  unsupported-claim boundaries.
