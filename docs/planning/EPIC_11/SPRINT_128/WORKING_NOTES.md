# Sprint 128 Working Notes

## Sprint Goal

Convert Sprint 127's remaining compatible/wide residual, wide nullspace,
threshold-family, SuiteSparse corpus, optional-large, exact minimum-norm, and
helper-movement deferred debt into bounded evidence or explicit future-owner
decisions before Q/economy, partial-SVD, corpus-index, performance, package,
and adoption work consume those truth boundaries.

## Starting Constraints

- Treat Sprint 127's residual deferred debt, final validation package, and
  non-claim register as the source of truth for Sprint 128 scope.
- Do not reopen completed Sprint 127 intake, compatible/wide residual policy,
  dependent-row projector evidence, perturbed duplicate-column threshold
  evidence, SuiteSparse corpus deferrals, SuiteSparse minimum-norm deferrals,
  exact 3 x 6 minimum-norm evidence, maintainer evidence, validation, or
  closeout work.
- Keep residual-only, nullspace/subspace, threshold-family, SuiteSparse corpus,
  optional-large, minimum-norm, QR-vs-SVD cross-check, exact-value, and helper
  movement lanes separate because each lane has different proof boundaries.
- Preserve non-claims around broad LAPACK, NumPy, SciPy, BLAS, SuiteSparse,
  PETSc, Trilinos, Eigen, ARPACK, vendor-backend, dense-library, QR,
  compatible solve, wide solve, rank-deficient solve, nullspace, minimum-norm,
  Q-basis, economy, sparse-mode, reorder, backend, corpus, optional-data,
  performance, platform, package, public API, and state-of-the-art parity.
- If any `.c` or `.h` file changes, run `make format && make lint && make
  test`. Documentation-only changes require `git diff --check` and focused
  markdown whitespace validation.

## Input Artifact Inventory

| Input | Role in Sprint 128 |
| --- | --- |
| `docs/planning/EPIC_11/PROJECT_PLAN.md` Sprint 128 | Defines seven Sprint 128 items for residual dedupe, compatible/wide residual evidence, wide/near-threshold subspace gates, remaining threshold families, SuiteSparse QR corpus gates, SuiteSparse and optional-large minimum-norm gates, and exact minimum-norm/helper movement gates. |
| `docs/planning/EPIC_11/SPRINT_128/PLAN.md` | Provides day-level execution order and 166-hour budget. |
| `docs/planning/EPIC_11/SPRINT_127/RETROSPECTIVE.md` | Defines the carry-forward residual deferred debt and explicit non-claim register. |
| `docs/planning/EPIC_11/SPRINT_127/WORKING_NOTES.md` | Captures completed Sprint 127 evidence, validation rules, duplicate fences, and future-owner handoffs. |
| Sprint 127 Day 1 artifact | Source for deferred dedupe and duplicate fences against completed Sprint 121-127 work. |
| Sprint 127 Day 2-3 artifacts | Source for compatible zero-residual and wide residual-only semantics and explicit deferral gates. |
| Sprint 127 Day 4-5 artifacts | Source for nullspace/subspace policy and completed dependent-row projector evidence. |
| Sprint 127 Day 6-7 artifacts | Source for threshold-family policy and completed perturbed duplicate-column threshold evidence. |
| Sprint 127 Day 8-9 artifacts | Source for SuiteSparse rank-deficient QR corpus gates and explicit deferrals. |
| Sprint 127 Day 10-11 artifacts | Source for SuiteSparse and optional-large minimum-norm gates and explicit deferrals. |
| Sprint 127 Day 12-13 artifacts | Source for exact minimum-norm and QR-vs-SVD/helper gates plus completed 3 x 6 exact minimum-norm evidence. |
| Sprint 127 Day 14 artifact | Source for final validation, maintainer evidence, non-claim register, and Sprint 128 handoff boundaries. |
| Sprint 121-126 artifacts | Source for earlier QR/SVD/rank taxonomy, external-reference lanes, nullspace policies, threshold policies, SuiteSparse gates, minimum-norm owner maps, QR-vs-SVD rules, and helper ownership decisions. |

## Day-Level Ownership

| Day | Owner Focus | Project-Plan Items |
| --- | --- | --- |
| 1 | Sprint intake, residual dedupe baseline, duplicate fence, validation boundary | Items 1-7 |
| 2 | Compatible zero-residual and wide residual-only semantics policy | Item 2 |
| 3 | Compatible/wide residual evidence decision or explicit deferral | Item 2 |
| 4 | Wide and near-threshold nullspace/subspace policy | Item 3 |
| 5 | Wide and near-threshold nullspace/subspace evidence batch or explicit deferral | Item 3 |
| 6 | Remaining threshold-family policy | Item 4 |
| 7 | Remaining threshold-family evidence batch or explicit deferral | Item 4 |
| 8 | SuiteSparse rank-deficient QR corpus gate | Item 5 |
| 9 | SuiteSparse rank-deficient QR corpus evidence batch or explicit deferral | Item 5 |
| 10 | SuiteSparse and optional-large minimum-norm policy | Item 6 |
| 11 | SuiteSparse and optional-large minimum-norm evidence decision | Item 6 |
| 12 | Exact minimum-norm cross-check and helper movement gate | Item 7 |
| 13 | Cross-check/helper implementation or explicit deferral plus integrated validation | Item 7 |
| 14 | Sprint closeout, non-claim update, and Sprint 129 handoff | Items 1-7 |

## Validation Expectations

| Change Type | Required Validation |
| --- | --- |
| Documentation only | `git diff --check` and focused markdown whitespace scan over Sprint 128 files. |
| `.c` or `.h` edits | `make format && make lint && make test`. |
| Python external-reference helper edits | `python3 -m py_compile` for the helper, focused helper invocation, affected test executable, and `git diff --check`. |
| Fixture or test registration edits | Focused executable proof plus Make/CMake/CTest impact check if membership changes. |
| SuiteSparse optional-corpus edits | Focused optional-data present/missing behavior, skip-path proof, diagnostics check, runtime note, and support-tier note. |
| Maintainer or public wording edits | Evidence-to-claim traceability, claim-boundary scan, link/path hygiene, and explicit non-claim update. |

## Scope Boundaries

- Sprint 128 may add bounded evidence only after the relevant trust,
  metadata, tolerance, skip, metric, diagnostics, support-tier, runtime, and
  failure interpretation are explicit.
- Sprint 128 may explicitly defer work when the future owner, dependency, and
  promotion gate are recorded.
- Sprint 128 must not relabel completed Sprint 127 fixtures as broader
  compatible, wide, multi-dimensional, threshold, SuiteSparse, optional-large,
  underdetermined, QR-vs-SVD, raw-basis, helper, or parity proof.
- Sprint 128 must not absorb Sprint 129 Q-basis/economy/helper ownership work
  except as prerequisite handoff data.
- Sprint 128 must not update public solver-selection wording unless Day 14
  proves evidence supports bounded user-facing wording beyond current
  guidance.

## Day 1 Notes

- Created the Sprint 128 working-notes baseline.
- Created the Sprint 128 artifact directory and Day 1 artifact entry.
- Mapped every Sprint 128 project-plan item to a day-level owner.
- Recorded duplicate fences for completed Sprint 121-127 QR residual,
  nullspace/subspace, threshold, minimum-norm, SuiteSparse, SVD-pseudoinverse,
  Q/economy, and helper evidence.
- Established validation expectations for documentation, C code, Python
  helper, fixture/test registration, SuiteSparse optional-corpus, maintainer,
  and public wording changes.

## Day 2 Notes

- Reviewed current compatible, dependent-row, wide, and residual-only QR
  evidence in `tests/test_qr_solve.c`, `tests/test_qr.c`,
  `tests/test_colamd.c`, `tests/qr_external_dense_reference.py`, and the
  maintained QR evidence row.
- Preserved `qr_rankdef_duplicate_5x4_residual_only` and
  `qr_rankdef_dependent_row_4x3_residual_only` as completed residual-only
  baselines, not candidates to repeat as Sprint 128 compatible zero-residual
  proof.
- Preserved `qr_overdetermined_compatible_5x3` and deterministic compatible
  tall solve checks as full-rank compatible evidence, not rank-deficient
  compatible evidence.
- Required any compatible zero-residual candidate to prove distinct trust
  beyond existing compatible solve, rank-only, residual-only, and
  rank-deficient solve-smoke evidence before Day 3 can accept code changes.
- Kept wide residual-only candidates deferred by default until
  underdetermined output semantics, solution-selection boundaries,
  Q/economy/sparse-mode boundaries, and residual-only proof value are pinned.
- Recorded candidate tables, output-semantics rules, residual tolerance and
  diagnostics policy, Day 3 acceptance checklist, future promotion gates, and
  explicit non-claims in the Day 2 artifact.

## Day 3 Notes

- Applied the Day 2 compatible zero-residual and wide residual-only semantics
  gate to all Sprint 128 Item 2 candidates.
- Explicitly deferred `qr_rankdef_duplicate_5x4_compatible_zero_residual`
  because it still does not add a distinct diagnostic beyond existing
  full-rank compatible solve, duplicate-column rank-only, duplicate-column
  residual-only, and rank-deficient solve-smoke evidence.
- Explicitly deferred `qr_rankdef_dependent_row_4x3_compatible_zero_residual`
  because Sprint 126 already accepted dependent-row residual-only evidence and
  Sprint 127 accepted dependent-row projector evidence.
- Explicitly deferred wide residual-only candidates because rank/nullity,
  underdetermined output semantics, solution-selection boundaries,
  Q/economy/sparse-mode boundaries, and residual-only proof value are not yet
  pinned tightly enough for code registration.
- Deferred SuiteSparse compatible or wide residual candidates to the Days
  8-11 corpus and minimum-norm gates.
- Did not change C, header, Python helper, Matrix Market, build, maintainer,
  public API, or public wording files.
- Recorded future-owner promotion gates, validation expectations, and residual
  non-claims in the Day 3 artifact.

## Day 4 Notes

- Refreshed the Sprint 125-127 nullspace/subspace policy for Sprint 128's
  remaining wide-shape, near-threshold, and SuiteSparse candidate set.
- Preserved `qr_rankdef_duplicate_5x4_nullspace_projector`,
  `qr_rank1_4x3_nullspace_projector`, and
  `qr_rankdef_dependent_row_4x3_nullspace_projector` as completed projector
  baselines, not candidates to repeat.
- Required every future subspace fixture to pin fixture key, shape, expected
  rank, expected nullity, threshold semantics, metric, tolerance, diagnostics,
  support tier when applicable, skip behavior, and proof boundary before
  implementation.
- Chose two-way projection residuals as the preferred metric for wide,
  near-threshold, and SuiteSparse candidates; full projectors remain
  acceptable only for tiny explicitly bounded fixtures.
- Deferred SuiteSparse subspace evidence by policy until independent expected
  rank/nullity metadata, support tier, optional-data behavior, runtime budget,
  and diagnostics are available.
- Recorded Day 5 candidate ordering, acceptance gates, deferral gates,
  validation requirements, and raw-basis/minimum-norm/Q-economy non-claims in
  the Day 4 artifact.

## Day 5 Notes

- Accepted `qr_rankdef_wide_3x5_nullspace_subspace` as the bounded wide-shape
  nullspace/subspace evidence batch for Sprint 128 Item 3.
- Reused the existing deterministic wide 3 x 5 rank-deficient matrix from
  `test_rank_rect_deficient`, with expected rank `2`, nullity `3`, and
  threshold `0.0`.
- Added a Python standard-library exact projector reference using the
  closed-form nullspace span generated by `[-2, 1, -3, 0, 0]`,
  `[-1, 0, 0, 1, 0]`, and `[0, 0, -2, 0, 1]`.
- Added a focused QR test that obtains the product nullspace basis,
  orthonormalizes it locally, compares its projector against the external
  reference, and reports projector diff, null residual, and orthogonality
  error.
- Updated `docs/maintainer_guide.md` to name the accepted bounded wide
  subspace fixture and keep the Sprint 127 dependent-row projector fixture
  visible in the maintained QR evidence row.
- Preserved raw basis equality, basis ordering, unique-basis, minimum-norm,
  pseudoinverse, Q-basis, economy, sparse-mode, SuiteSparse, optional-data,
  platform, performance, public API, and broad QR non-claims.
- Deferred near-threshold, SuiteSparse, sparse-mode, economy, principal-angle,
  and raw-basis lanes to their future-owner gates.
- Focused validation passed: helper emitted `OK 29`; `make build/test_qr &&
  ./build/test_qr` passed 73 tests with projector diff `4.441e-16`, null
  residual `8.671e-16`, and orthogonality error `4.441e-16`.
- Because Day 5 changed `.c` and Python helper files, full `make format &&
  make lint && make test` validation was required before closeout and passed.

## Day 6 Notes

- Reviewed completed Sprint 125-127 threshold-family fixtures:
  `qr_rank_threshold_diag4_family`,
  `qr_rank_threshold_diag4_scaled_family`, and
  `qr_rank_threshold_duplicate_5x4_perturbed_family`.
- Preserved those completed threshold fixtures as baselines, not candidates to
  relabel as dependent-row, wide, default-threshold, SuiteSparse, residual,
  subspace, or minimum-norm evidence.
- Required remaining threshold candidates to pin fixture key, matrix family,
  parameters, thresholds, strict comparison rule, expected ranks, diagnostics,
  support tier when applicable, skip behavior, and proof boundary before code
  edits.
- Selected `qr_rank_threshold_dependent_row_4x3_perturbed_family` as the
  preferred Day 7 candidate only if controlled perturbation metadata and
  stable expected ranks can be pinned without reusing residual or projector
  metrics as threshold proof.
- Kept wide threshold, default-threshold, SuiteSparse threshold, and
  near-threshold subspace lanes deferred by policy unless Day 7 can satisfy
  their stricter metadata and non-claim gates.
- Recorded candidate ordering, diagnostics, support-tier requirements,
  failure interpretation, validation rules, and global threshold/default
  threshold non-claims in the Day 6 artifact.

## Day 7 Notes

- Implemented the preferred Day 6 candidate
  `qr_rank_threshold_dependent_row_4x3_perturbed_family` as a bounded
  dependent-row threshold fixture.
- Added a Python external-reference helper record with perturbation `1e-6`,
  thresholds `1e-10`, `1e-8`, and `1e-6`, and fixture-local expected ranks
  `3`, `3`, and `2`.
- Added product QR evidence in `tests/test_qr.c` comparing
  `sparse_qr_rank()` and `sparse_qr_rank_info()` against the helper output
  while printing relative threshold, absolute threshold, expected rank,
  product rank, rank-info rank, pivot ratio, and R diagonal magnitudes.
- Updated the maintainer guide QR row so threshold ownership includes the
  completed perturbed duplicate-column and dependent-row threshold fixtures.
- Explicitly deferred wide threshold, default-threshold, SuiteSparse threshold,
  and near-threshold subspace lanes because their Day 6 promotion gates remain
  stricter than the bounded Day 7 implementation batch.
- Preserved the non-claim boundary: the new fixture proves only
  fixture-local explicit-threshold rank behavior for one perturbed
  dependent-row matrix.

## Day 8 Notes

- Re-read Sprint 126 and Sprint 127 SuiteSparse rank-deficient QR corpus gates
  and the Sprint 127 explicit deferral decision before changing Sprint 128
  policy.
- Refreshed the checked-in Matrix Market inventory from
  `tests/data/suitesparse/*.mtx`; the current set remains `west0067`, `nos4`,
  `bcsstk04`, `steam1`, `fs_541_1`, `orsirr_1`, `bcsstk14`, `s3rmt3m3`,
  `Kuu`, `bloweybq`, `Pres_Poisson`, and `tuma1`.
- Preserved `west0067.mtx`, `nos4.mtx`, and `bcsstk04.mtx` as existing QR
  controls, not rank-deficient corpus evidence.
- Kept every remaining default, optional-large, and report-only SuiteSparse
  candidate behind independent expected-rank or threshold/rank metadata,
  threshold semantics, runtime budget, support-tier, skip, diagnostics, and
  validation gates.
- Did not change C tests, Python helpers, Matrix Market data, optional-data
  gates, public wording, or maintainer claims for Day 8.
- Recorded Day 9's two valid paths: promote one candidate only if all metadata
  gates are satisfied before code edits, or explicitly defer the corpus lane.

## Day 9 Notes

- Reviewed the Day 8 SuiteSparse rank-deficient QR corpus gate and found no
  candidate with independent expected-rank or threshold/rank metadata.
- Explicitly deferred SuiteSparse rank-deficient QR corpus evidence rather
  than registering product QR observations as expected values.
- Ran `make build/test_qr_solve && ./build/test_qr_solve` as focused control
  diagnostics; the existing SuiteSparse QR controls reported `nos4` rank
  `100`, `bcsstk04` rank `132`, and `west0067` rank `67`.
- Preserved `west0067.mtx`, `nos4.mtx`, and `bcsstk04.mtx` as full-rank QR
  controls, not rank-deficient corpus evidence.
- Kept `steam1.mtx`, `fs_541_1.mtx`, `orsirr_1.mtx`, and report-only corpus
  matrices deferred until independent expected-rank metadata, support-tier,
  runtime, skip, diagnostics, and validation gates are satisfied.
- Did not change C tests, Python helpers, Matrix Market data, optional-data
  gates, public wording, or maintainer claims for Day 9.

## Day 10 Notes

- Reviewed Sprint 125-127 minimum-norm artifacts before defining the Sprint
  128 SuiteSparse and optional-large minimum-norm policy.
- Preserved the accepted Sprint 125 `west0067.mtx` first-30-row smoke as the
  only checked-in SuiteSparse minimum-norm corpus baseline: extraction
  `rows[0..29]` into a `30 x 67` system, RHS `b = A * ones`, residual
  `< 1e-8`, positive `||x_min||`, and `||x_min|| <= ||ones|| + 1e-8`.
- Rejected repeating the same `west0067` smoke as new Sprint 128 evidence
  unless a future owner adds a distinct, pinned metric.
- Required any additional SuiteSparse or optional-large minimum-norm candidate
  to pin matrix path, extraction rule, shape, nnz, RHS, expected rank/nullity
  when claimed, residual metric/tolerance, norm comparison target,
  support-tier, skip behavior, runtime/platform expectations, diagnostics, and
  validation before code edits.
- Kept `steam1`, `fs_541_1`, `orsirr_1`, report-only matrices,
  rank-deficient SuiteSparse minimum-norm, and SuiteSparse QR-vs-SVD corpus
  cross-checks deferred unless Day 11 can satisfy the full metadata gate.
- Preserved SVD-pseudoinverse as a bounded cross-check only; no broad
  QR/SVD, SuiteSparse, optional-large, platform, or performance claim is
  introduced by Day 10.

## Day 11 Notes

- Reviewed the Day 10 SuiteSparse and optional-large minimum-norm policy and
  selected explicit deferral for additional corpus evidence.
- Ran `make build/test_colamd && ./build/test_colamd` to preserve focused
  minimum-norm owner diagnostics; the owner suite passed 70 tests, 0 failures,
  0 skips, and 317 assertions.
- Recorded the current `west0067` first-30-row smoke diagnostic:
  `30 x 67`, max residual `1.78e-15`, `||x_min||=4.30`, and
  `||ones||=8.19`.
- Preserved the existing `west0067` smoke as the only accepted checked-in
  SuiteSparse minimum-norm baseline and rejected repeating it as new Sprint
  128 evidence.
- Deferred new `west0067` extractions, `steam1` submatrices, optional-large
  `fs_541_1` and `orsirr_1` submatrices, report-only matrices,
  rank-deficient SuiteSparse minimum-norm, and SuiteSparse QR-vs-SVD corpus
  cross-checks until extraction, rank/nullity, residual, norm, skip, runtime,
  support-tier, and validation metadata are pinned.
- Did not change C tests, Python helpers, Matrix Market data, optional-data
  gates, public wording, or maintainer claims for Day 11.

## Day 12 Notes

- Reviewed Sprint 125-127 exact minimum-norm, QR-vs-SVD, and helper movement
  artifacts before setting the Sprint 128 Day 13 gate.
- Preserved completed exact-value baselines:
  `qr_underdetermined_minnorm_2x4`,
  `qr_minnorm_5x10_exact_values`, `qr_minnorm_3x6_exact_values`,
  `test_minnorm_1xn`, `test_minnorm_with_colamd`,
  `test_minnorm_rank_deficient`, and `test_minnorm_zero_row`.
- Preserved Sprint 125 `test_minnorm_vs_pinv` as the only accepted
  QR-vs-SVD minimum-norm cross-check and kept SVD-pseudoinverse wording
  bounded to named fixtures.
- Rejected duplicate 3 x 6, 5 x 10, and 2 x 4 exact-value work as Sprint 128
  implementation candidates.
- Required any Day 13 exact-lane promotion to provide a non-duplicate shape,
  fixture key, closed-form expected values, exact norm, residual/value/norm
  tolerances, diagnostics, owner-local placement, and full validation plan
  before code edits.
- Deferred helper movement unless the helper name is behavior-specific,
  owner call sites keep fixture keys and tolerances visible, and focused plus
  full validation gates are known.

## Day 13 Notes

- Applied the Day 12 exact minimum-norm, QR-vs-SVD cross-check, and helper
  movement gate.
- Explicitly deferred new `3 x 6`, `5 x 10`, and `2 x 4` QR-vs-SVD or exact
  minimum-norm lanes because Sprint 125-127 already own the completed bounded
  evidence for those fixtures.
- Explicitly deferred larger synthetic exact underdetermined evidence because
  no non-duplicate fixture key, closed-form expected vector, exact norm,
  tolerance policy, diagnostics, and owner-local placement were pinned before
  code edits.
- Explicitly deferred SuiteSparse-derived exact and QR-vs-SVD lanes because
  Days 10-11 limited SuiteSparse minimum-norm evidence to the accepted
  `west0067` smoke without promoting corpus oracle claims.
- Explicitly deferred generic minimum-norm helper consolidation; future helper
  movement remains gated on behavior-specific names, visible owner call-site
  tolerances, and focused plus full validation.
- Did not change C tests, headers, Python helpers, Matrix Market data, build
  files, maintainer guide text, or public API wording for Day 13.
- Integrated validation passed for the Sprint 128 touched QR helper fixtures,
  the QR executable, the QR solve executable, and the COLAMD/minimum-norm
  executable.

## Day 14 Notes

- Reconciled all seven Sprint 128 project-plan items against the day-level
  artifacts.
- Published the final evidence index covering Days 1-14.
- Recorded accepted Sprint 128 code evidence:
  `qr_rankdef_wide_3x5_nullspace_subspace` and
  `qr_rank_threshold_dependent_row_4x3_perturbed_family`.
- Recorded explicit deferrals for compatible zero-residual, wide residual-only,
  near-threshold subspace, SuiteSparse subspace, wide/default/SuiteSparse
  threshold families, SuiteSparse rank-deficient QR corpus, additional
  SuiteSparse/optional-large minimum-norm lanes, additional exact
  underdetermined lanes, additional QR-vs-SVD cross-checks, and generic helper
  consolidation.
- Added blocker and future-owner statements for each deferred item.
- Refreshed the final Sprint 128 non-claim register and Sprint 129 handoff
  notes so Q-basis, economy, and helper ownership work can begin without
  reopening Sprint 128 claim boundaries.
- Did not change C tests, headers, Python helpers, Matrix Market data, build
  files, public API wording, or maintainer guide text for Day 14.
