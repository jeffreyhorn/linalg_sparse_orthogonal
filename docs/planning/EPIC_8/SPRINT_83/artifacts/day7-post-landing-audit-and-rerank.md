# Sprint 83 Day 7: Post-Landing Audit and Rerank

## Purpose

Re-rank the strongest remaining Sprint 83 contradiction after the Day 6 shared
matrix-shell scalar-surface landing.

## Main Result

The Day 6 landing closed the strongest first contradiction:

- the shared matrix-shell public seam no longer reads as a raw-`double`
  outlier
- a second immediate matrix-shell scalar batch is not the highest-value next
  move

The strongest remaining Sprint 83 seam is now the touched-path index / ABI
follow-through lane.

## Exact Next Center

The exact Day 8 design center is now fixed to:

- `include/sparse_types.h`

That shared vocabulary owner still reads primarily as the iterative/eigs
public scalar seam even though Day 6 widened the shared matrix-shell helper
seam. The strongest residual contradiction is therefore a shared-vocabulary
and package-visible interpretation gap, not a missing second matrix-shell code
batch.

## Support-Only Follow-Through

The strongest support-only follow-through is now:

- `tests/test_sparse_matrix.c`
- `README.md`
- `docs/maintainer_guide.md`

Current reading:

- `tests/test_sparse_matrix.c` already owns the strongest direct proof for the
  landed shared matrix-shell scalar seam
- `README.md` remains broadly truthful, but it is the strongest package-visible
  wording surface if Day 8 needs explicit support movement
- `docs/maintainer_guide.md` already carries the strongest authoritative
  maintainer-policy split for the landed seam

## Preserved Non-Touch Map

The useful Day 7 clarification is explicit now:

- `include/sparse_qr.h` and `include/sparse_svd.h` remain later
  algorithm-surface widening seams, not the next move
- `include/sparse_cholesky.h` and `include/sparse_ldlt.h` remain support-only
  family-local public surfaces for this sprint lane
- direct-family implementation owners do not reopen here
- package/install/export mechanics still do not justify their own Sprint 83
  batch absent a real touched public-contract move

## Strongest Clarification

Sprint 83's next contradiction center is no longer “more scalar widening on
the matrix shell.” It is the stale shared vocabulary and touched-path ABI
reading left behind after the first landing.

That fixes the ordering:

- next seam = index / ABI follow-through
- later seam = bounded algorithm-surface widening
- not the other way around

## Exit State

- Sprint 83 now has one explicit post-Day-6 rerank.
- Day 8 can stay bounded to one index / ABI follow-through design lane.
- Support drift is separated from the real next capability move.
