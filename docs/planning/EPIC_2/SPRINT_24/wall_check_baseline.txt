# Sprint 24 wall-check baseline.
#
# `make wall-check` (defined in top-level Makefile, executed via
# scripts/wall_check.sh) reads this file at run-time and compares the
# observed `reorder_ms` against `2 × <baseline>` for each line.  >2×
# the baseline → exit non-zero.  Format: `KEY=VALUE_MS`, one per line;
# `#`-prefixed lines and blanks are ignored.
#
# Day 1 initial values (Sprint 23 worst-case ceilings, leaving room for
# items 2-5 to reduce them):
#
#   bcsstk14_qg_amd_ms = 7000
#       Sprint 23 Day 12 measured 6 951 ms (`bench_summary_day12.md "(b)"`).
#       Sprint 22 quotient-graph baseline was ~140 ms.  Item 2's fix
#       targets ≤ 1.5× Sprint 22 baseline = ~210 ms; once it lands on
#       Day 4, this baseline gets bumped down to the post-fix
#       measurement so Days 5-12 work against the tighter gate.
#
#   pres_poisson_amd_ms = 1100000
#       Sprint 23 Day 12 measured 1 339 904 ms.  Sprint 22 baseline
#       was 12 209 ms.  Item 2's fix targets ≤ 1.5× Sprint 22
#       baseline = ~18 300 ms.  Day 4 bumps this baseline once the
#       fix is verified.
#
# Sprint 24 Day 4 (post-item-2 fix) updates this file with the new
# tighter ceilings, with a `# previous values` comment block recording
# the worst-case originals so a future regression that exceeds 2× the
# new ceiling but stays under the old one is still caught.

bcsstk14_qg_amd_ms=7000
pres_poisson_amd_ms=1100000
